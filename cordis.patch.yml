# dsh-maestro-mobile bundle patch: one client-only plugin row.
# The browser half is discovered through the package.json dsh.client declaration;
# the host half is an empty apply so the row exists in the host Loader.
- insert:
    - id: dsh-maestro-mobile
      name: '@ddtcorex/dsh-maestro-mobile'
